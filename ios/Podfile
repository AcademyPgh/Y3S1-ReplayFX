# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MillvaleMusicFestival' do
  pod 'AppCenter/Crashes', '~> 1.13.0'
  pod 'AppCenter/Analytics', '~> 1.13.0'
  pod 'AppCenterReactNativeShared', '~> 1.12.0'
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for MillvaleMusicFestival

  

  platform :ios, '9.0'
  target 'MillvaleMusicFestivalTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'MillvaleMusicFestival-tvOS' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for MillvaleMusicFestival-tvOS

  target 'MillvaleMusicFestival-tvOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
